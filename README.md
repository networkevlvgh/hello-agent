# Hello Agent

A minimal Node.js agent using the OpenAI API.

## Setup

1. Install dependencies:

```bash
npm install
```

2. Create a `.env` file:

```env
OPENAI_API_KEY=your_api_key_here
```

3. Run the agent:

```bash
node index.mjs
```
